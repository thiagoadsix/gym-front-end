import { styled } from "..";

export const FormSection = styled("div", {
  flex: "1",
  display: "flex",
  justifyContent: "center",
  alignItems: "center",
});

export const ImageSection = styled("div", {
  flex: 1,
  display: "flex",
  justifyContent: "center",
  alignItems: "center",
});
